name: 'Create Pull Request'
on:
  pull_request:
    types: [opened, synchronize]

jobs:
  create:
    runs-on: ubuntu-18.04
    name: Create PR job
    steps:
      - uses: actions/checkout@v2
      - name: create pr step
        id: test
        uses: betrybe/create-pr-from-files-action@test-pull-request
        with:
          token: ${{ secrets.MY_TOKEN }}
          owner: 'betrybe'
          repo: 'my-target-repo'
          branch: ${{ github.event.pull_request.head.ref }}
          storagePath: './tmp1'
